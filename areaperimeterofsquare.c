#include<stdio.h>
int main()
{
      int side, area, perimeter;
      printf("enter the value of length:");
      scanf("%d" , &side);
      area = side * side;
      perimeter = 4*side;
      printf("%d\n", area);
      printf("%d\n", perimeter);
      return 0;
      }











